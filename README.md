# MNIST-CNN-Keras
```
    classifier = Predict()

    import base64_image
    image = base64_image.eight

    from helper import Helper
    result = classifier.predict(Helper.read_img(image))

    print(result)
```
